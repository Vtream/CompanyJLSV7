@model IEnumerable<CompanyJLSV7.Models.Facturas>

@{
    ViewBag.Title = "Facturas";
}

<h2>Facturas</h2>

<p>
    @Html.ActionLink("Agregar Factura", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>


        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>


        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Facturas1 }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.Facturas1 }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.Facturas1 })
        </td>
    </tr>
}

</table>
